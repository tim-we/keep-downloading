{
    "private": true,
    "scripts": {
        "build": "webpack --config webpack.config.js --progress",
        "build:watch": "webpack --config webpack.config.js --progress --watch",
        "run": "web-ext run --source-dir ext-files",
        "build:ext": "webpack --config webpack.config.js && web-ext build --source-dir ext-files"
    },
    "author": "tim-we",
    "license": "MIT",
    "devDependencies": {
        "@types/firefox-webext-browser": "^109.0.0",
        "ts-loader": "^9.4.2",
        "typescript": "^4.9.4",
        "webpack": "^5.75.0",
        "webpack-cli": "^5.0.1"
    }
}
